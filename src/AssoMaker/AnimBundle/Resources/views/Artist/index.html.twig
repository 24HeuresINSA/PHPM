{% extends "AssoMakerBaseBundle::layout.html.twig" %}

{% block body -%}
    <h1>Artist list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Stage</th>
                <th>Horaires</th>
                <th>Mainpictureurl</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('artist_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.stage }}</td>
                <td>{{ entity.horaires }}</td>
                <td>{{ entity.mainPictureUrl }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('artist_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('artist_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('artist_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
