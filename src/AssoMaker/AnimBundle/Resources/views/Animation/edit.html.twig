{% extends "AssoMakerBaseBundle::layout.html.twig"  %}
{% set pageTitle = entity.nom %}

{% block content %}
<form class="form-inline" action="{{ path('anim_animation_new') }}" method="post" {{ form_enctype(form) }}>
    <div class="row-fluid">

        <div class="span6">
    		<fieldset>
                
                    <div class="row-fluid">
                    <legend>Animation</legend>
                        <div class="row-fluid">
                            <div class="span6">{{ form_row(form.nom) }}</div>
                            <div class="span6">{{ form_row(form.equipe) }}</div>
                        </div>
                        <div class="row-fluid">
                            <div class="span6">{{ form_row(form.responsable) }}</div>
                            <div class="span6">{{ form_row(form.orgaManif) }}</div>
                        </div>
                    </div>
            
            </fieldset>
            <fieldset>

            
            </fieldset>
            
        
        
            
            	        
        </div>
        <div class="span6">
            <div class="row-fluid">
                <legend>Intervenant Extérieur</legend>
                <div class="row-fluid">
                    <div class="span6">{{ form_row(form.extType) }}</div>
                    <div class="span6">{{ form_row(form.extNom) }}</div>
                </div>
                <div class="row-fluid">
                    <div class="span6">{{ form_row(form.extTelephone) }}</div>
                    <div class="span6">{{ form_row(form.extEmail) }}</div>
                </div>
            </div>
            <div class="row-fluid">
                <div class="span12">{{ form_widget(form.extCommentaire) }}</div>

            </div>
        </div>
        
        {{ form_widget(form) }}
    </div>	
    
    <div class="form-fluid">
			<div class="form-actions">
			    <button class="btn btn-primary" type="submit">Enregistrer les modifications</button>
			</div>
    </div>       
</form>
{% endblock %}