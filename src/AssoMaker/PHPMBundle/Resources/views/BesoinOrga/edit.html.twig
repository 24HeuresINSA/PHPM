<h1>BesoinOrga edit</h1>

<form action="{{ path('besoinorga_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit">Edit</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('besoinorga') }}">
            Back to the list
        </a>
    </li>

</ul>
