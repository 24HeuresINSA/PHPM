{% extends "AssoMakerPHPMBundle::layout.html.twig"  %}
{% set pageTitle = 'Rapport d\'affectation' %}

{% block pageHeader %}
{% endblock %}

{% block content %}
    
	    <div class="row-fluid">       
	    	<div class="page-header">
	         	<h1>{{ entitiesMacro.orga(orga) }} - rapport d'affectation auto</h1>
	         </div>
	    </div>

	    
	    <div class="row-fluid"> 
			<table class="table table-condensed">
			    <thead>
			        <tr>
			            <th>Créneau</th>
						<th>Message</th>
			        </tr>
			    </thead>
			    <tbody>
				    {% for e in messages %}
			           <tr>
			              <td>{{ e.creneau.debut | format_datetime('EEEE dd MMM Y HH:mm') }} <i class="icon-arrow-right"></i> {{ e.creneau.fin | format_datetime('EEEE dd MMM Y HH:mm') }}
			              {{ entitiesMacro.tache(e.creneau.plageHoraire.tache) }}</td>
			              <td>{{ e.msg }}</td>
{# 			              <td>#}
{# 			              {% if e.creneau.disponibilite %}#}
{# 			              <a class="btn" href="{{ path('creneau_desaffecter', { 'cid': e.creneau.id }) }}"><i class="icon-pencil"></i> F</a>#}
{# 			              {% endif %}#}
{# 			              </td>#}
			           </tr>
				     {% endfor %}
			    </tbody>
			</table>
			
	
			
		</div>
		
		<div class="form-fluid">
			<div class="form-actions">
				<div class="btn-toolbar">
					<a class="btn btn-primary" href="{{ path('orga_affectation', { 'orga_id': orga.id }) }}"><i class="icon-book"></i>Ouvrir dans la vue affectation</a>
				</div>
				
			</div>
	    </div>

{% endblock %}


