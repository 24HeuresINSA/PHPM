parameters:
    quartHeureValidator.class:      PHPM\Validator\quartHeureValidator

services:
    quartHeure:
       class:      %quartHeureValidator.class%
       arguments: [@doctrine.orm.entity_manager]
       tags:
            - { name: validator.constraint_validator,, alias: validator.quartHeure } 
