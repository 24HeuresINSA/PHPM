{% extends "PHPMBundle::layout.html.twig"  %}
{% set pageTitle = 'Edition Mission' %}
{% use "PHPMBundle::edit.html.twig" %}

{% block content %}
<form action="{{ path('groupedi_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Valider</button>
    </p>
</form>
{% endblock %}