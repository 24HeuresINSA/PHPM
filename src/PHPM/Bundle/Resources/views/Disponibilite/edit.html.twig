{% extends "::base.html.twig"  %}
{% block content %}
{% block title %}
	Disponibilit&eacute;s -> Edition
{% endblock %}

<form action="{{ path('disponibilite_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit">Edit</button>
    </p>
</form>
{% endblock %}

{% block menu_action %}
<ul class="record_actions">
    <li>
        <a href="{{ path('disponibilite') }}">
            Retourner &agrave; la liste
        </a>
    </li>
    <li>
        <form action="{{ path('disponibilite_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
