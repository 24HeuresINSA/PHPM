<table>
    <thead>
        <tr>
            <th>Groupe de Tâches</th>
            <th>Équipe</th>
            <th>Responsable</th>
            <th>Tâches</th>
            
            
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td>{{ entitiesMacro.groupeTache(entity) }}</td>
            <td>{{ entity.equipe.nom }}</td>
            <td>{{ entitiesMacro.orga(entity.responsable) }}</td>
            <td>
            <ul>
           {% for tache in entity.taches %}
            <li >{{ entitiesMacro.tache(tache) }}{{ entitiesMacro.tacheCom(tache) }}</li>
            {% endfor %}
            </ul></td>

        </tr>
    {% endfor %}
    </tbody>
</table>

