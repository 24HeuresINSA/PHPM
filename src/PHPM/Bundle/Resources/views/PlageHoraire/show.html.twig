{% extends "PHPMBundle:PlageHoraire:layout.html.twig"  %}
{% set pageTitle = 'Détails de la plage horaire' %}
{% use "PHPMBundle::show.html.twig" %}

{% block content %}
<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Debut</th>
            <td>{{ entity.debut|date('Y-m-d H:i:s') }}</td>
        </tr>
        <tr>
            <th>Fin</th>
            <td>{{ entity.fin|date('Y-m-d H:i:s') }}</td>
        </tr>
         <tr>
            <th>T&acirc;che</th>
            <td>{{ entity.tache }}</td>
        </tr>
         <tr>
            <th>NbOrgasNecessaires</th>
            <td>{{ entity.nbOrgasNecessaires }}</td>
        </tr>
         <tr>
            <th>Dur&eacute;</th>
            <td>{{ entity.dureeCreneau }}</td>
        </tr>
        <tr>
                    <th>Recoupement</th>
                    <td>{{ entity.recoupementCreneau }}</td>
        </tr>
    </tbody>
</table>
{% endblock %}