{% extends "::base.html.twig"  %}

{% block title %}
	Cr&eacute;neau -> Edition
{% endblock %}

{% block content %}
<form action="{{ path('creneau_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit">Edit</button>
    </p>
</form>
{% endblock %}

{% block menu_action %}
<ul class="record_actions">
    <li>
        <a href="{{ path('orga_show', { 'id': entity.disponibilite.orga.id }) }}">
            Revenir &agrave; l'orga assign&eacute;
        </a>
    </li>
    <li>
    	<a href="javascript:creneau_delete('{{ path('orga_delete', { 'id': entity.id }) }}')">Supprimer</a>
    </li>
</ul>
{% endblock %}